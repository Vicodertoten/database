# INT-004 — Consumer Closure On Published Runtime Fixtures

## ID

INT-004

## Title

Consumer Closure On Published Runtime Fixtures

## Status

closed

## Objective

Record owner-side traceability for the consumer execution of INT-004 in `runtime-app`, based on runtime fixtures published in INT-003.

## Repo owner

- Repo: database
- Reason: owner of runtime data truth and official fixture publication.

## Repo consumer

- Repo: runtime-app
- Reason: consumer integration and validation of runtime fixture consumption.

## Dependency

- INT-003 closed owner-side (`database`) and published:
  - `fixtures/runtime/playable_corpus.sample.json`
  - `fixtures/runtime/pack_compiled.sample.json`
  - `fixtures/runtime/pack_materialization.sample.json`

## What happened in INT-004 (consumer-side)

- `runtime-app` imported owner fixtures into `apps/api/fixtures/`.
- `runtime-app` added `apps/api/src/tests/contracts.integration.test.ts`.
- Consumer validation passed in `runtime-app` commit `36a8741`:
  - `corepack pnpm --filter @runtime-app/contracts run build`
  - `corepack pnpm --filter @runtime-app/api run test:contracts`
  - `pnpm run check`

## Decisions locked

- INT-004 was executed consumer-side from owner-published fixtures.
- No new runtime surface was introduced.
- No owner-side fixture payload update was required.
- No schema, pipeline, contract, or business logic change was required in `database`.

## Out of scope

- Any change to `fixtures/runtime/*`
- Any schema change
- Any pipeline change
- Any runtime session logic

## Handoff note

INT-004 inter-repo closure is recorded owner-side with consumer proof (`runtime-app` commit `36a8741`).
Next exact step: INT-005.

## Closure summary

Closed 2026-04-17. `runtime-app` consumed the published INT-003 owner fixtures successfully and validated real runtime surface ingestion via integration tests, without requiring any owner data-truth change.
