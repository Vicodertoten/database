# INT-004 — Consumer integration closure on owner fixtures

## ID

INT-004

## Title

Consumer integration closure on owner fixtures

## Status

closed

## Objective

Record owner-side traceability for the consumer execution of INT-004 in `runtime-app`, based on fixtures officially published in INT-003.

## Why this chantier exists

INT-004 was executed consumer-side in `runtime-app`.
`database` must close inter-repo traceability without changing any owner data truth.

## Repo owner

- Repo: database
- Reason: owner of runtime data truth and published fixture surfaces.

## Repo consumer

- Repo: runtime-app
- Reason: consumer integration and validation of owner fixtures.

## Dependency

- INT-003 is closed owner-side and published the official fixture trio in `fixtures/runtime/`.

## Source of truth

- `docs/runtime_consumption_v1.md`
- `docs/20_execution/chantiers/INT-003.md`
- `fixtures/runtime/playable_corpus.sample.json`
- `fixtures/runtime/pack_compiled.sample.json`
- `fixtures/runtime/pack_materialization.sample.json`
- `runtime-app` commit: `36a8741`

## What happened in INT-004 (consumer-side)

- `runtime-app` imported the three official owner fixtures under `apps/api/fixtures/`.
- `runtime-app` added `apps/api/src/tests/contracts.integration.test.ts`.
- Consumer validations passed (`contracts` build, fixture contract test, monorepo check).

## Decisions

- No new runtime data surface was introduced.
- No owner-side fixture payload was modified.
- No schema, pipeline, contract, or business logic change was required in `database`.
- INT-004 is considered closed inter-repo from owner traceability perspective.

## Out of scope

- Any change to `fixtures/runtime/*`
- Any schema change
- Any pipeline change
- Any runtime session logic

## Handoff note

INT-004 is closed with consumer proof on commit `36a8741`.
Next exact step is `INT-005`.

## Closure summary

Closed 2026-04-17.
`runtime-app` successfully consumed owner fixtures published in INT-003 and validated real runtime surface ingestion through integration tests, without requiring owner-side data truth changes.
