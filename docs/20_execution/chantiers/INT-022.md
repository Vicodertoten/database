# INT-022 - Phase 6 pilot-prep hardening alignment

## Status

- in_progress
- opened: 2026-04-20

## Scope

Synchronize owner-side `database` execution posture with runtime phase 6 hardening:

- align cross-repo execution docs/handoff with runtime `INT-021` baseline and active `INT-022`
- provide owner-side operational readiness evidence hooks for pilot runbook
- keep strict owner perimeter unchanged (no institutional ownership drift)

## Owner-side commitments

- preserve runtime-read service boundary to official 3 surfaces only
- preserve editorial-write transport boundary to pack/enrichment operations only
- provide health diagnostics and request observability for owner services
- support dry-run evidence collection (`health`, error categories, latency logs)

## Cross-repo synchronization requirements

- `database/docs/20_execution/handoff.md` and `integration_log.md` must reflect active `INT-022`
- runtime-app and database chantier briefs must carry matching acceptance targets:
  - SLO `99.5% / 7d`
  - latency `p95 <= 800ms`
  - pilot load profile `100` concurrent learners
  - `2` complete dry-runs with one simulated owner incident minimum

## Explicit non-scope

- no runtime session/progression logic in owner repo
- no new owner contract version bump
- no LMS or institutional backend expansion in `database`

## Verification commands

- `ruff check .`
- `python -m compileall -q src tests/test_runtime_read_owner_service.py tests/test_editorial_write_owner_service.py`
