# INT-016 — Runtime-read owner-side proof hardening (Phase 6)

## ID
INT-016

## Title
Runtime-read owner-side proof hardening (Phase 6)

## Status
closed

## Repo owner
`database`

## Repo consumer
`runtime-app`

## Objective
Strengthen owner-side proof quality for runtime-read transport on non-trivial cases:
- series behavior on compiled/materialization reads
- explicit HTTP error semantics (`400`/`404`/`500`)
- schema non-regression on served payloads

## Scope (locked)
- extend `tests/test_runtime_read_owner_service.py`
- keep transport boundaries unchanged (read-only, 3 official surfaces only)
- no new runtime contract version

## Delivered scope
- owner-read seeding now covers a build/materialization series (latest revision path proven)
- service-level assertions now validate both revision-targeted and latest compiled retrieval
- HTTP server assertions now include:
  - `GET /packs/{pack_id}/compiled` latest behavior
  - `400 invalid_limit` on bad playable-corpus limit
  - `400 invalid_revision` on bad revision parsing
  - `500 internal_error` mapping for owner service failures

## Validation executed
- `python -m pytest -q tests/test_runtime_read_owner_service.py -p no:capture` passed

## Closure summary
Closed 2026-04-19.

Owner-side read transport proof now covers series + error matrix explicitly while preserving the same bounded runtime-read doctrine.
