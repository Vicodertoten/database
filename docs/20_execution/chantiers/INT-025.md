# INT-025 - Phase 2 pre-filtrage image amont (reduction cout IA)

## Status

- closed_no_go
- opened: 2026-04-22
- closed: 2026-04-22

## Scope

Execute only Phase 2 pre-AI filtering in owner `database`:

- reject non-eligible images before AI qualification
- keep runtime/data boundaries unchanged
- keep contract changes additive only
- publish observable pre-AI rejection signals

## Locked decisions applied

- blur: active directly (no shadow mode)
- dedup: exact hash only (`sha256`), no `pHash` in Phase 2
- dimensions gate: reuse existing AI min dimensions (`MIN_AI_IMAGE_WIDTH/HEIGHT`)
- no schema/runtime contract change
- no remediation scope (Phase 4/5 topics remain out-of-scope)

## Implementation evidence

Code changes:

- `src/database_core/adapters/inaturalist_snapshot.py`
  - additive media manifest fields: `blur_score`, `pre_ai_rejection_reason`
  - snapshot summary includes `pre_ai_rejection_reason_counts`
- `src/database_core/adapters/inaturalist_harvest.py`
  - computes lightweight `blur_score` on downloaded image
  - keeps existing `sha256`/dimensions signals
- `src/database_core/adapters/inaturalist_qualification.py`
  - pre-AI decision point before Gemini call
  - enforced reasons:
    - `insufficient_resolution_pre_ai`
    - `decode_error_pre_ai`
    - `blur_pre_ai`
    - `duplicate_pre_ai`
  - filtered media are not sent to Gemini
  - AI outputs remain replayable with explicit status per filtered media
- `src/database_core/qualification/policy.py`
  - maps new pre-AI reasons to fast-screening review semantics
- `src/database_core/ops/smoke_report.py`
  - adds additive field `pre_ai_rejection_reason_counts`
- `src/database_core/cli.py`
  - qualification CLI now reports `pre_ai_rejected` count

## Validation evidence

Tests:

- `python -m pytest -q -p no:capture tests/test_inat_snapshot.py tests/test_cli.py tests/test_smoke_report.py`
  - passed (`43 passed`)
- Added coverage includes:
  - insufficient resolution pre-AI skip
  - decode error pre-AI skip
  - blur pre-AI skip
  - duplicate pre-AI skip
  - eligible media non-regression path
  - summary distribution for `pre_ai_rejection_reason_counts`

Operational checks:

- `python scripts/verify_goldset_v1.py` passed
- `python scripts/generate_smoke_report.py --snapshot-id inaturalist-birds-v2-20260421T210221Z --fail-on-kpi-breach` passed (with `.env` database URL)
- `corepack pnpm --filter @runtime-app/web run test:smoke` passed (`3/3`)
- `python scripts/verify_repo.py` fails on pre-existing doc marker expectation (`Politique distracteurs v2`) unrelated to INT-025 scope

Linked commits:

- database: `b37cc09`
- runtime-app: `42eaa8e`

Comparable smoke artifacts (current state capture):

- `docs/20_execution/phase2/smoke_run1.smoke_report.v1.json`
- `docs/20_execution/phase2/smoke_run2.smoke_report.v1.json`
- `docs/20_execution/phase2/smoke_run3.smoke_report.v1.json`
- `docs/20_execution/phase2/decision_summary.v1.json`

## Decision evidence (real A/B baseline vs candidate)

Decision source:

- `docs/20_execution/phase2/decision_summary.v1.json`

Observed real A/B result (15 taxa, same live-frozen snapshot payload):

- baseline: `processed=150`, `sent_to_gemini=145`, `estimated_ai_cost_eur=0.174`, `exportable=128`
- candidate: `processed=150`, `sent_to_gemini=145`, `estimated_ai_cost_eur=0.174`, `exportable=128`
- `pre_ai_rejection_reason_counts` candidate: `{"insufficient_resolution_pre_ai": 5}`
- delta:
  - `sent_to_gemini = 0`
  - `estimated_ai_cost_eur = 0`
  - `exportable = 0` (non-regression respected)

## Exit decision

- status: `NO_GO`
- rationale:
  - implementation and compatibility objectives are met
  - no regression on locked KPIs and exportable output
  - measurable AI-call/cost reduction is not demonstrated on real A/B same-snapshot evidence

## Follow-up (outside INT-025 closure)

- run a targeted corpus with non-zero `blur_pre_ai`, `decode_error_pre_ai`, or `duplicate_pre_ai`
- publish refreshed delta table and promote only if cost reduction is measurable with exportable non-regression
