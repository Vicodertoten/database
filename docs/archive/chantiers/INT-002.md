# INT-002 — Align runtime-app/packages/contracts with official database schemas

## ID

INT-002

## Title

Align runtime-app/packages/contracts with official database schemas

## Status

closed

## Objective

Make `runtime-app/packages/contracts` a 1:1 consumer mirror of the three official runtime schemas defined in `database`:

- `playable_corpus.v1` — `schemas/playable_corpus_v1.schema.json`
- `pack.compiled.v1` — `schemas/pack_compiled_v1.schema.json`
- `pack.materialization.v1` — `schemas/pack_materialization_v1.schema.json`

Eliminate all local field drift (e.g. `contract`, `generatedAt`, `packVersion`) and replace with the exact field names mandated by the owner schemas.

## Why this chantier exists

INT-001 locked the doctrinal boundary. INT-002 enforces it at the TypeScript contract level.
`runtime-app/packages/contracts` currently uses simplified or invented field names that do not match the actual JSON output of `database`.
Any runtime loader built on those drifted types will silently mis-parse real artifacts.
The fix must flow from the owner schema → consumer types, not from runtime-side preference.

## Repo owner

- Repo: database
- Reason: the three JSON schemas are the authoritative, versioned source of truth for field names, types, and required properties.

## Repo consumer

- Repo: runtime-app
- Reason: `packages/contracts` types must derive from the owner schemas without adding, renaming, or removing fields.

## Source of truth

- `schemas/playable_corpus_v1.schema.json`
- `schemas/pack_compiled_v1.schema.json`
- `schemas/pack_materialization_v1.schema.json`
- `docs/runtime_consumption_v1.md`

## Known field drift to correct (from owner schema → current consumer)

### playable_corpus.v1

Required envelope fields in schema: `schema_version`, `playable_corpus_version`, `generated_at`, `run_id`, `items`

### pack.compiled.v1

Required envelope fields in schema: `schema_version`, `pack_compiled_version`, `build_id`, `pack_id`, `revision`, `built_at`, `question_count_requested`, `question_count_built`, `distractor_count`, `source_run_id`, `questions`

### pack.materialization.v1

Required envelope fields in schema: `schema_version`, `pack_materialization_version`, `materialization_id`, `pack_id`, `revision`, `source_build_id`, `created_at`, `purpose`, `ttl_hours`, `expires_at`, `question_count`, `questions`

## Files to read first

- README.md
- docs/README.md
- docs/runtime_consumption_v1.md
- docs/20_execution/handoff.md
- docs/20_execution/chantiers/INT-002.md
- schemas/playable_corpus_v1.schema.json
- schemas/pack_compiled_v1.schema.json
- schemas/pack_materialization_v1.schema.json

## Files to read first in the consumer repo

- README.md
- packages/contracts/src/ (all contract type files)
- docs/20_execution/chantiers/INT-002.md (when created)

## Planned actions

1. Re-read the three owner schemas to extract all required fields and their exact names
2. Audit `runtime-app/packages/contracts` for every drift from those fields
3. Rewrite the drifted TypeScript types to match the schemas 1:1
4. Verify that all runtime loaders and consumers of those types still compile cleanly
5. Run consumer-side tests to confirm no silent regressions
6. Update `runtime-app/docs/20_execution/chantiers/INT-002.md` to `closed`
7. Update `runtime-app/docs/20_execution/handoff.md` and `integration_log.md`

## Risks

- consumer codebase may have many call sites depending on the drifted field names — scope may be larger than expected
- renaming fields may silently break runtime loaders if there are no type-level usages at the call site
- ensure fix flows strictly owner schema → consumer types, not consumer preference → schema proposal

## Acceptance criteria

- `runtime-app/packages/contracts` types match the owner schemas field-for-field
- no invented or renamed fields remain in the consumer types
- all TypeScript compilation and runtime tests pass
- no schema changes introduced in `database`
- no code changes introduced in `database`

## Verification commands

- database side: no verification needed (no changes here)
- consumer side: `tsc --noEmit` in `runtime-app/packages/contracts`
- consumer side: full test suite in `runtime-app`

## Out of scope

- schema changes in `database`
- new contract design or API redesign
- runtime session logic
- editorial operation contracts
- adding new fields not already in the owner schemas

## Owner validation note

Schemas verified 2026-04-15 as the authoritative contract source of truth.
No schema changes were needed or made.
The three files below are stable and make law:

- `schemas/playable_corpus_v1.schema.json`
- `schemas/pack_compiled_v1.schema.json`
- `schemas/pack_materialization_v1.schema.json`

No local field renaming is permitted on the consumer side.

## Handoff note

Owner (database) step: schemas confirmed as stable official source of truth — complete.
Consumer (runtime-app) step: align `packages/contracts` types field-for-field with these schemas, then close the chantier.

## Closure summary

Closed 2026-04-15 after final inter-repo validation.

- Owner source of truth explicitly locked on the three schema files:
	- `schemas/playable_corpus_v1.schema.json`
	- `schemas/pack_compiled_v1.schema.json`
	- `schemas/pack_materialization_v1.schema.json`
- Consumer contracts in `runtime-app/packages/contracts` were aligned field-for-field with no local renaming and no semantic transformation (runtime-app commit `1868923`).
- Consumer boundary reminder is present in `runtime-app/packages/contracts/README.md`.
- Required consumer verification command passed: `npx -y pnpm run check` (exit 0).
- No schema refactor, pipeline change, or runtime logic change introduced in `database`.
