# INT-024 - Phase 1 instrumentation et baseline KPI

## Status

- closed_go_with_gaps
- opened: 2026-04-22
- closed: 2026-04-22

## Scope

Execute Phase 1 instrumentation only:

- extend smoke report with additive non-blocking KPI signals
- expose compile deficits summary for operator visibility
- publish a comparable baseline over 3 consecutive runs
- keep locked KPI semantics and `overall_pass` unchanged

## Locked decisions applied

- no schema/runtime contract change
- additive-only `smoke.report.v1` policy:
  - no rename of historical fields
  - no removal of historical fields
  - no semantic change on historical fields
- `kpis` remains the locked KPI registry
- new Phase 1 metrics are published under `extended_kpis`
- `overall_pass` remains computed only from locked KPI passes

## Implementation evidence

Code:

- `src/database_core/ops/smoke_report.py`
  - added `extended_kpis`
  - added `compile_deficits_summary`
  - kept historical `kpis` and `overall_pass` behavior
- `src/database_core/storage/postgres.py`
  - added `fetch_phase1_smoke_metrics()`
  - computes:
    - `taxon_playable_coverage_ratio`
    - `taxon_with_min2_media_ratio`
    - `country_code_completeness_ratio`
    - `distractor_diversity_index`
  - computes compile deficits rollup from latest compilation attempts
- `src/database_core/storage/services.py`
  - exposed `fetch_phase1_smoke_metrics()` on `PostgresPipelineStore`

Tests:

- `tests/test_smoke_report.py`
  - validates additive contract and new Phase 1 fields
  - preserves locked KPI registry expectations

## Baseline runs (comparable)

Snapshot/policy:

- snapshot: `inaturalist-birds-v2-20260421T210221Z`
- same command profile on 3 consecutive runs
- same formulas and report version

Artifacts:

- `docs/20_execution/phase1/smoke_run1.smoke_report.v1.json`
- `docs/20_execution/phase1/smoke_run2.smoke_report.v1.json`
- `docs/20_execution/phase1/smoke_run3.smoke_report.v1.json`
- `docs/20_execution/phase1/baseline_summary.v1.json`

Observed baseline (stable across runs):

- `taxon_playable_coverage_ratio = 0.875`
- `taxon_with_min2_media_ratio = 0.7625`
- `country_code_completeness_ratio = 0.0`
- `distractor_diversity_index = 0.21978`
- `overall_pass = true` (locked KPI set)

## Verification

- `python -m pytest -q -p no:capture tests/test_smoke_report.py` passed (3 passed)
- `python scripts/verify_goldset_v1.py` passed
- `corepack pnpm --filter @runtime-app/web run test:smoke` passed (3/3)
- `python scripts/verify_repo.py` failed on pre-existing doc marker check in `tests/test_verify_repo.py`:
  - expected marker: `Politique distracteurs v2` in `docs/codex_execution_plan.md`
  - unrelated to Phase 1 instrumentation changes

## Exit decision

- status: `GO_WITH_GAPS`
- rationale:
  - instrumentation baseline objective achieved
  - additive smoke compatibility preserved
  - comparability demonstrated on 3 runs
  - extended KPI gaps remain open for subsequent corrective work
