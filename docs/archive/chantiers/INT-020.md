# INT-020 - Support owner-side pour surface editoriale legere runtime (Phase 4)

## Status

- closed
- date: 2026-04-19

## Scope

Supporter la livraison d'une surface editoriale minimale cote `runtime-app` sans etendre le perimetre owner-side:

- garder le service write strictement borne pack/enrichment
- clarifier les refus invalides via validations explicites
- garantir la stabilite des contrats write versionnes existants

## Implemented

- fix serveur write owner-side:
  - `src/database_core/editorial_write/http_server.py`
  - correction parsing compile (`revision`) + validations coherentes
- hardening tests:
  - `tests/test_editorial_write_owner_service.py`
  - cas invalides explicites ajoutes pour:
    - `diagnose` revision invalide
    - `compile` question_count invalide
    - `materialize` purpose invalide
    - `enqueue` question_count invalide
    - `execute` payload invalide (valeur et type)

## Verification

- `ruff check .`
- `python -m compileall -q src tests/test_editorial_write_owner_service.py`

## Notes

- aucun changement de version de contrat.
- aucun glissement vers logique session/progression/utilisateur.
- `database` reste owner strict; runtime-app orchestre via `apps/api`.
